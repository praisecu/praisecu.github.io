<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">

  <meta
    name="viewport"
    content="width=device-width, initial-scale=1"
  >

  <title>{{ title }} | PRAISe Lab at CU Boulder</title>

  <meta
    name="description"
    content="{{ description | default('PRAISe Lab at the University of Colorado Boulder.') }}"
  >

  <meta property="og:title" content="{{ title }} | PRAISe Lab">
  <meta
    property="og:description"
    content="{{ description | default('PRAISe Lab at the University of Colorado Boulder.') }}"
  >
  <meta
    property="og:image"
    content="https://praisecu.com/img/logo/praise-logo.webp"
  >

  <link rel="icon" type="image/x-icon" href="/img/logo/praise.webp">

  <link
    rel="stylesheet"
    href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css"
  >

  <link
    rel="stylesheet"
    href="https://fonts.googleapis.com/css?family=Open+Sans:300,400,600"
  >

  <link
    rel="stylesheet"
    href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@300;400;500;600;700;800;900&display=swap"
  >

  <link rel="stylesheet" href="/css/team.css">
  <link rel="stylesheet" href="/css/bootstrap.min.css">
  <link rel="stylesheet" href="/fonts/flaticon/flaticon.css">
  <link rel="stylesheet" href="/css/font-awesome.min.css">
  <link rel="stylesheet" href="/css/hippo-off-canvas.css">
  <link rel="stylesheet" href="/css/animate.css">
  <link rel="stylesheet" href="/css/language-select.css">
  <link rel="stylesheet" href="/owl.carousel/assets/owl.carousel.css">
  <link rel="stylesheet" href="/css/magnific-popup.css">
  <link rel="stylesheet" href="/css/menu.css">
  <link rel="stylesheet" href="/css/template.css">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/responsive.css">
  <link rel="stylesheet" href="/css/research.css">
  <link rel="stylesheet" href="/css/praise-style.css">
  <link rel="stylesheet" href="/css/cards.css">

  <!-- Load last so it overrides legacy page styles. -->
  <link rel="stylesheet" href="/css/markdown-pages.css">
  <link rel="stylesheet" href="/css/modern-header.css">
  <link rel="stylesheet" href="/css/modern-footer.css">
  <link rel="stylesheet" href="/css/teaching-page.css">

  <script
    src="https://kit.fontawesome.com/4ad8d74216.js"
    crossorigin="anonymous"
  ></script>

  <script src="/js/vendor/modernizr-2.8.1.min.js"></script>
  {% include "components/seo.njk" %}

  {% include "components/analytics.njk" %}

  {% include "components/pwa-head.njk" %}
</head>

<body>
{% include "components/modern-header.njk" %}
  <main class="modern-page teaching-page">
  <section class="modern-page-hero">
    <div class="modern-page-container">
      <div class="modern-page-heading">
        {% if kicker %}
          <p class="modern-page-kicker">{{ kicker }}</p>
        {% endif %}

        <h1 class="modern-page-title">{{ title }}</h1>

        {% if summary %}
          <p class="modern-page-summary">{{ summary }}</p>
        {% endif %}
      </div>
    </div>
  </section>

  <section class="teaching-page-section">
    <div class="teaching-page-container">
      {% for term in teaching_terms %}
        <section
          id="{{ term.id }}"
          class="teaching-term"
          aria-labelledby="{{ term.id }}-title"
        >
          <header class="teaching-term__heading">
            <p class="teaching-term__eyebrow">
              Course Offerings
            </p>

            <h2
              id="{{ term.id }}-title"
              class="teaching-term__title"
            >
              {{ term.name }}
            </h2>
          </header>

          <div class="teaching-list">
            {% for course in term.courses %}
              <article
  class="teaching-course{% if course.media_fit == 'contain' %} teaching-course--banner{% endif %}"
>
                <a
                  class="teaching-course__media{% if course.media_fit == 'contain' %} teaching-course__media--contain{% endif %}"
                  href="{{ course.url }}"
                  aria-label="View {{ course.title }}"
                >
                  {% if course.media_type == "video" %}
                    <video
                      autoplay
                      muted
                      loop
                      playsinline
                      disablepictureinpicture
                      preload="metadata"
                      poster="{{ course.media | replace('.mp4', '-poster.jpg') }}"
                      tabindex="-1"
                      aria-label="{{ course.media_alt }}"
                    >
                      <source
                        src="{{ course.media }}"
                        type="video/mp4"
                      >
                    </video>
                  {% else %}
                    <img
                      src="{{ course.media }}"
                      alt="{{ course.media_alt }}"
                      loading="lazy"
                    >
                  {% endif %}
                </a>

                <div class="teaching-course__body">
                  {% if course.code %}
                    <p class="teaching-course__code">
                      {{ course.code }}
                    </p>
                  {% endif %}

                  <h3 class="teaching-course__title">
                    <a href="{{ course.url }}">
                      {{ course.title }}
                    </a>
                  </h3>

                  {% if course.subtitle %}
                    <p class="teaching-course__subtitle">
                      {{ course.subtitle }}
                    </p>
                  {% endif %}

                  <p class="teaching-course__description">
                    {{ course.description }}
                  </p>

                  <a
                    class="teaching-course__button"
                    href="{{ course.url }}"
                  >
                    <i
                      class="fa fa-paperclip"
                      aria-hidden="true"
                    ></i>

                    <span>Course Details</span>
                  </a>
                </div>
              </article>
            {% endfor %}
          </div>
        </section>
      {% endfor %}

      <aside class="teaching-archive">
        <div>
          <p class="teaching-archive__eyebrow">
            Course Archive
          </p>

          <h2 class="teaching-archive__title">
            Previous Courses
          </h2>

          <p class="teaching-archive__text">
            View courses offered before joining the University of
            Colorado Boulder.
          </p>
        </div>

        <a
          class="teaching-archive__button"
          href="{{ previous_courses_url }}"
          target="_blank"
          rel="noopener noreferrer"
        >
          View Previous Courses
        </a>
      </aside>
    </div>
  </section>
</main>

{% include "components/edit-page.njk" %}

  {% include "components/modern-footer.njk" %}
  <script src="/js/jquery.js"></script>
  <script src="/js/bootstrap.min.js"></script>
  <script src="/owl.carousel/owl.carousel.min.js"></script>
  <script src="/js/jquery.magnific-popup.min.js"></script>
  <script src="/js/hippo-offcanvas.js"></script>
  <script src="/js/jquery.inview.min.js"></script>
  <script src="/js/jquery.stellar.js"></script>
  <script src="/js/jquery.countTo.js"></script>
  <script src="/js/classie.js"></script>
  <script src="/js/selectFx.js"></script>
  <script src="/js/jquery.sticky-kit.min.js"></script>
  <script src="/js/scripts.js"></script>
  <script src="/js/modern-header.js"></script>
</body>
</html>


